// so i'm SUPER not sold on this but it... sorta makes sense?
// like, I can't use an enum here because then the hasTag/tags[] api would have to accept that enum
// and idk how that would work. same with string literal types
export const block = 'block';
export const enemy = 'enemy';
export const bullet = 'bullet';
export const staticBlock = 'staticBlock';
export const player = 'player';